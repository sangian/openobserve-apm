# Traefik Static Configuration
# =============================================================================
# NOTE: Most configuration is provided via CLI arguments in docker-compose.traefik.yml.
# This file only contains settings that are easier to manage in YAML format.
# Do NOT duplicate CLI args here — Traefik v3 does not resolve shell/env variables
# in static config files (e.g. ${VAR} is passed literally, not substituted).
# =============================================================================

# Global configuration
global:
  checkNewVersion: true
  sendAnonymousUsage: false
